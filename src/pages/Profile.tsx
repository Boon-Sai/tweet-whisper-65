import { Layout } from '../components/Layout';

const Profile = () => {
  return (
    <Layout>
      <div className="p-4">
        <h1 className="text-2xl font-bold">Profile</h1>
        <p className="text-muted-foreground">This is the Profile page.</p>
      </div>
    </Layout>
  );
};

export default Profile;
